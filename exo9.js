const Stack = require("./Stack");

function min(stack) {
    if(!stack.isEmpty) {

    }
}

function max(stack) {
    if(!stack.isEmpty) {
    }
}

const stack = new Stack();
stack.push("Andrea")
stack.push("Serrano")
console.log(stack.items);
min(stack)
